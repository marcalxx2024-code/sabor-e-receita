import { EmptyState } from '../components/common/EmptyState';import { RecipeCard } from '../components/recipes/RecipeCard';import { useFavorites } from '../contexts/FavoritesContext';import { recipes } from '../data/recipes';import { useDocumentTitle } from '../hooks/useDocumentTitle';
export function FavoritesPage(){useDocumentTitle('Favoritos');const {favoriteIds}=useFavorites();const list=recipes.filter(r=>favoriteIds.includes(r.id));return <section className="container-app py-12"><h1 className="text-4xl font-black md:text-5xl">Suas receitas favoritas</h1><p className="mt-3 text-stone-600">Sua coleção fica salva neste navegador.</p><div className="mt-9">{list.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{list.map(r=><RecipeCard key={r.id} recipe={r}/>)}</div>:<EmptyState title="Sua coleção está vazia" description="Favorite receitas para encontrá-las rapidamente depois." link="/receitas"/>}</div></section>}
