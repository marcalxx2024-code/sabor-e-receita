import { ArrowRight } from 'lucide-react';import { Link } from 'react-router';import { categories, recipes } from '../data/recipes';import { useDocumentTitle } from '../hooks/useDocumentTitle';
export function CategoriesPage(){useDocumentTitle('Categorias');return <section className="container-app py-12"><h1 className="text-4xl font-black md:text-5xl">Categorias</h1><p className="mt-3 text-stone-600">Encontre a receita certa para cada momento.</p><div className="mt-9 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{categories.map(c=>{const list=recipes.filter(r=>r.category===c);return <Link key={c} to={`/receitas?categoria=${encodeURIComponent(c)}`} className="group relative min-h-72 overflow-hidden rounded-3xl"><img src={list[0].image} alt="" className="absolute inset-0 h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/80 to-black/10"/><div className="absolute inset-x-0 bottom-0 p-6 text-white"><span className="text-sm">{list.length} receitas</span><h2 className="mt-1 text-3xl font-black">{c}</h2><span className="mt-3 inline-flex items-center gap-2 font-bold">Explorar <ArrowRight size={18}/></span></div></Link>})}</div></section>}
